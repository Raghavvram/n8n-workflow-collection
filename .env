# Sets the number of SQLite database connections in the pool.
# A higher number allows more concurrent DB queries but uses more resources.
export DB_SQLITE_POOL_SIZE=5

# Enables the runner feature for executing tasks in isolated environments or containers.
# Useful for distributed or scalable workflow execution setups.
export N8N_RUNNERS_ENABLED=true

# Controls whether workflow nodes can access environment variables.
# Set to false if your workflows need to read environment variables.
export N8N_BLOCK_ENV_ACCESS_IN_NODE=false

# Prevents use of bare Git repositories for version control integration.
# Recommended to keep it true to avoid data corruption or permission issues.
export N8N_GIT_NODE_DISABLE_BARE_REPOS=true

# Sets the timezone used by n8n for timestamps, logs, and scheduling.
export GENERIC_TIMEZONE="Asia/Kolkata"

# Enables community-contributed n8n nodes and packages.
# Allows installation from the n8n community package registry.
export N8N_COMMUNITY_PACKAGES_ENABLED="true"

# Enables installation and use of officially verified n8n packages.
# Verified packages are reviewed and considered safe to use.
export N8N_VERIFIED_PACKAGES_ENABLED="true"

# Allows installation of unverified, third-party packages.
# Useful for testing custom or experimental nodes, but may reduce security.
export N8N_UNVERIFIED_PACKAGES_ENABLED="true"

# Enables running Python scripts using the n8n Python integration feature.
# Required if using the Python node for data processing or automation logic.
export N8N_PYTHON_ENABLED="true"

# Controls whether session cookies are secured (HTTPS only).
# Set to true in production with HTTPS; false allows local HTTP development.
export N8N_SECURE_COOKIE="false"

# Forces strict permissions on the n8n settings file.
# Set to false for flexibility during development; true for production security.
export N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS="false"
